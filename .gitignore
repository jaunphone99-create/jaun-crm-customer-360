# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# Claude config
.claude/

# Environment variables
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Build output
dist/
build/

# ข้อมูลตัวอย่างที่สร้างได้ (14 MB · deterministic) — สร้างด้วย npm run db:seed
supabase/seed.sql

# Next.js (โฟลเดอร์ web/)
.next/
next-env.d.ts
*.tsbuildinfo

# ฐานข้อมูลทดลองในเครื่อง (tools/db/dev-api.mjs) — สร้างใหม่ได้ด้วย npm run dev:db:fresh
.dev-db/
.dev-db-verify/

# CSS ที่คัดลอกมาจาก prototype ตอน build (web/scripts/sync-design.mjs)
web/src/app/generated/
web/public/fonts/

# ---------------------------------------------------------------------------
# ความลับ — ห้าม commit เด็ดขาด (Direction 23 ก.ย. 2569 · CANONICAL ข้อ 20.20)
# ใช้ Environment/Secret Store เท่านั้น
# ---------------------------------------------------------------------------
# ไฟล์ตั้งค่าของ Smoke Test บน environment จริง — มี cookie/JWT ของบัญชีพนักงานจริง
# ใครได้ไฟล์นี้ไป = สวมสิทธิ์คนนั้นได้ทันทีจนกว่าเซสชันจะหมดอายุ
*.smoke.json
pilot.json
staging.json
prod.json

# สถานะการ link ของ Supabase CLI — มี project ref และ token ของเครื่องนั้น
supabase/.temp/
supabase/.branches/

# ไฟล์ .env ทุกแบบ (นอกเหนือจากที่ประกาศไว้ข้างบน)
.env.*
!.env.example

# คีย์และใบรับรอง
*.pem
*.key
*.p12
*.pfx
service-account*.json
